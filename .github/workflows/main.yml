on:
 push:
   branches:
     - master








job:
 build:
   runs-on: ubuntu latest

   steps:
     - name: 'Testing Github actions'
       run : pwd

     - name: 'Testing printing'
       run: echo raneem  
